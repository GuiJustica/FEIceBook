version: '3.8'

services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbit
    ports:
      - "5672:5672"     # AMQP
      - "15672:15672"   # painel de gerência
    networks:
      - dist-net

networks:
  dist-net:
    driver: bridge
